---
name: execute-plan
description: "按已批准计划单流执行任务，并在每一步后验证状态。"
---

Invoke the `.agents/skills/superpowers/skills/executing-plans/SKILL.md` workflow and follow it exactly.

GRPD override: execute one implementation task at a time, keep changes scoped, and use the relevant GRPD specialist skill when touching C++ kernels, materials, solvers, schemas, CMake, or postprocessing.
